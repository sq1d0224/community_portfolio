<h1>投稿の編集</h1>

<%= render 'form', post: @post %>

<div class="actions">
  <%= link_to 'キャンセル', @post, class: 'btn btn-secondary' %>
  <%= link_to 'Topへ', top_path, class: 'btn btn-primary ml-2' %>
</div>
