<div class="page-background">
  <div class="posts-page">
    <h1 class="posts-title">コメントした投稿一覧</h1>

    <%= render 'shared/search_form', search_path: commented_posts_posts_path %>

    <div class="communities-actions">
      <%= link_to '投稿一覧', posts_path, class: 'btn btn-primary' %>
    </div>

    <div class="posts-grid">
      <% if @posts.any? %>
        <% @posts.each do |post| %>
          <%= render 'shared/post_item', post: post %>
        <% end %>
      <% else %>
        <div class="no-comment">
          <p class="no-posts">コメントした投稿がありません。</p>
        </div>
      <% end %>
    </div>

    <%= render 'shared/pagination', items: @posts %>
  </div>
</div>
